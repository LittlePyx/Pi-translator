# Pi Translator

Pi Translator 是由 P&I Lab 开发的 Microsoft Edge Manifest V3 扩展，用于翻译 Overleaf、PDF 或普通网页中选中的文本。它支持用户自备的 OpenAI 兼容 API、LaTeX 结构保护、浮动翻译卡片、右键菜单和快捷键。

## 当前功能

- Overleaf 划词后显示翻译按钮。
- 普通网页与 Overleaf 可从快捷面板、网页内固定翻译侧栏或 Edge 浏览器侧栏顶部直接进入一次性“框选网页区域”：适合文字、图像、图表、Canvas、渲染公式和复杂混排。用户主动确认框选后，扩展始终只截取当前可见页中的框内画面，并交给多模态模型理解和翻译；普通划词翻译仍使用文字接口，两种入口不会混用。从 Edge 浏览器侧栏首次启动时，Edge 会询问是否授予可选的“所有网站”访问权限；这是 `captureVisibleTab` 在侧栏不具备 `activeTab` 时要求的浏览器能力。Pi Translator 自身仍把该入口限制为普通 HTTP/HTTPS 页面，只有用户允许后才能直接截取网页画面。若不授权，仍可从工具栏快捷面板使用一次性的 `activeTab` 框选。框选阶段提供高对比十字准星并降低遮罩强度；两种侧栏入口在空闲、翻译中和查看已有译文时都保持可见。选框可移动并从四角缩放，结果或失败后可恢复原框继续调整，也可重新框选；鼠标之外还支持 `Enter` 建框、方向键移动、`Shift + 方向键` 缩放和 `Ctrl + 方向键` 细调。每次调整后仍需再次确认，密码、验证码和支付字段始终禁止发送；不支持整页、自动滚动或连续截图。
- 内置轻量 PDF 阅读器，支持本地 PDF 和在线 PDF 的文字划选、跨行框选与流式翻译。
- Pi PDF 支持 `Ctrl/Cmd + F` 在本机渐进搜索整份 PDF：结果按页码显示一行原文上下文并局部高亮，可用鼠标或方向键跳转，长文档只渲染当前附近的结果项。扫描件不会因搜索自动触发 OCR；用户主动“识别本页”后，该页仅存在于当前标签页的临时文字层会自动加入搜索。
- Pi PDF 提供紧凑的“导航”入口：PDF 自带章节目录时可在“目录 / 书签”之间切换，多级章节支持展开、收起、当前章节跟随和键盘跳转；没有内置目录时直接显示阅读书签，不根据字号猜测标题。用户可一键收藏当前页，为书签重命名或添加简短备注，再按页码跳转和删除；同一页不会重复添加，每篇最多 50 个、最多保留最近 30 篇 PDF。书签只按匿名文档标识保存在本机，不保存 PDF 文件名、网址、正文或截图；小屏幕继续使用覆盖式面板，不长期挤压正文宽度。
- Pi PDF 会随页面懒加载一层受控链接：文内章节、脚注、图表引用等内部目标在阅读器内跳转，DOI 和论文主页等明确的 `http/https` 链接由用户点击后在新标签页打开。内部跳转后会显示一个轻量“返回第 N 页”入口，最多保留当前文档最近 20 个返回点，缩放后仍可继续返回，换文档立即清除且不会写入存储。链接只在悬停或键盘聚焦时轻微强调；不执行 PDF 脚本、表单、附件、启动动作或危险协议。
- Pi PDF 使用同一个框选入口智能选择处理方式：框内存在可靠文字层时在本地提取文字并走文字翻译，只有扫描版、乱码或文字覆盖不足时才把确认的局部截图发送给视觉模型。选框可移动、四角可缩放，识别或提取原文默认折叠显示并可复制或修正后仅重译；结果中的页码可返回并短暂高亮原区域，菜单可重新识别或恢复原选框继续调整。
- PDF 框选翻译支持当前浏览器会话内去重：相同截图、模型和翻译设置可复用最近结果，“重新识别”会明确绕过缓存。缓存只保存截图哈希、识别原文和译文，不保存截图、Data URL 或 Base64 图片。
- Pi PDF 会根据文字块之间的实际留白识别明显双栏：普通划选意外带入另一栏时保留起始栏，明确拖入另一栏时保留原选区并按 PDF 原始文字顺序发送，不自行重排正文。拖选轨迹实际经过的通栏公式、标题或图注会作为中性内容保留，不会被左右栏过滤误删；无法形成连续原生选区时也不会跨过另一栏强行拼接。翻译按钮旁会先显示一行实际选区预览；连续翻译侧栏为避免误发，跨栏时仍优先收紧到起始栏。
- Pi PDF 会保守识别多行、多列的表格型选区：划词顺序可能不可靠时可从选区预览一键改为可调整框选，再核对范围并发送；连续翻译侧栏不会自动发送这类选区。判定要求稳定网格证据，普通双栏正文和单行并排内容继续按原有划选方式处理。
- 保留 Edge 原生 PDF 阅读器：选中文字后右键翻译，结果会在 Edge 侧边栏流式显示；侧边栏仅提示可按需用 Pi PDF 打开，不会自动跳转或替换原生阅读器。
- PDF 页面按视口附近懒加载，只保留当前页附近 5 页的 Canvas 与文字层，并限制单页画布像素；支持紧凑页码跳转、适合宽度和保持页内位置的缩放。鼠标位于 PDF 文档上时，`Ctrl + 滚轮`或触控板捏合只缩放 PDF，不缩放整张阅读器页面。Pi PDF 会按匿名文档标识在本机记住最近 30 份文档的页码、页内位置、缩放和侧栏状态，不保存原始网址、文件名或正文。扫描版缺少文字层时只显示一次轻量提示，并提供“识别本页”快捷入口：它先生成可调整的近整页选框，仍需用户确认才会发送；官方 Qwen 接口可调用专用 OCR 生成仅限当前标签页的临时可选文字层，缩放后自动重建，换 PDF 后清除。
- 普通网页右键或快捷键按需翻译，默认不申请所有网站的长期权限。
- 普通网页可从快捷面板或 Edge 浏览器侧栏显式开启“翻译网页正文”：扩展识别主要文章区域，保留原文并把译文插在对应段落下方；只先处理当前视口及下一屏，滚动后继续。导航、表单、表格、代码块和独立公式会被跳过，内联代码会随上下文保留。任务可暂停、停止后继续或一键清除，译文只存在于当前标签页，不写入划词历史，也不用于 Overleaf 编辑区或 PDF。
- 单词或短语等短选区会优先显示当前语境译义，并可按接口返回补充读音、词性和少量常用释义；可一键切换回普通译文。原文朗读仅使用浏览器明确标记为本地的系统语音。
- 可选择仅 Overleaf、普通网页按需、指定网站自动启用或所有网站自动启用。
- 文字翻译 API 按“服务商 → API Key → 自动读取模型”完成连接；普通网页划词、Overleaf 划词和可选文字 PDF 只需这一项。网页框选与扫描件框选需要支持图片输入的多模态模型，“识别本页”需要官方 Qwen / 阿里云百炼；设置页提供按需的 Qwen 入口，自定义 API 仍可填写 Base URL 和模型名称。
- 最多保存 6 组 API 配置，并从工具栏快速切换。
- 使用当前 API Key 自动读取接口可用模型。
- 可用一张不含用户内容的内置小图单独测试视觉模型能力，并区分权限、Key、余额、模型和网络错误。
- 兼容性诊断会检查模型列表、鉴权、对话接口、结构化输出和逐句能力。
- 快捷面板用一条低干扰状态行分别显示文字翻译与图像翻译配置状态；只检查本机配置、Key 是否存在、域名权限和已有能力记忆，不会为了显示状态自动调用 API。异常项可直接进入对应的 Key、模型或授权设置，图像翻译未配置时普通文字翻译仍可正常使用。
- 快捷面板会同时核对文字翻译配置、Edge 实际分配的快捷键和当前页面可用性；仅在需要处理时展开相应动作，全部正常时收为一行就绪状态。PDF 来源与文件权限继续使用当前文档对应的精确指引。
- 源语言、目标语言、翻译风格和内容模式设置。
- 工具栏快速面板可切换目标语言，并临时暂停当前网站的自动划词。
- 普通网页高置信度判断选区已是目标语言时，可自动隐藏浮动按钮。
- 自动、纯文本和 LaTeX 三种内容模式。
- 译文中的行内和独立 LaTeX 公式可在输出完成后自动渲染；只有检测到公式时才显示小型“源码/公式”切换按钮，复制操作始终保留可编辑的 LaTeX 源码。渲染器随扩展本地打包，不加载 CDN 或远程代码，渲染失败时自动回退源码。
- API Key 默认仅保存到浏览器会话，也可选择本机持久保存。
- 复制译文和重新翻译。
- 翻译完成后可从复制按钮旁的轻量“修正”入口直接修改译文，不会再次调用翻译 API。编辑器只开放自然语言片段，LaTeX 公式和命令以只读块锁定，保存时后台还会再次校验公式没有被增删、改写或换序。译文可选择“仅本次”或“记住本文”；固定术语默认收起且完全可选，可独立保存到本文或全局，扩展不会从整句修改中自动猜测术语。最近一次修正可撤销；若对应术语随后已被用户另行修改，撤销只恢复译文，不覆盖新的术语设置。同一标签页的翻译、修正和撤销会按顺序提交，过期编辑器或旧结果不能覆盖当前译文。
- 翻译历史可按原文、译文、网站和语言即时搜索；最近三版译文会概括相对当前版本的增删与调整，结果卡也会列出本次实际采用的固定术语。
- 本文术语可标记为“待核对”，并与公式、OCR、模型调整等事项集中显示在轻量的“本文待核对”入口；处理完毕后自动收起。
- 完整译文与逐句对照即时切换；逐句原文来自本地选区，不由模型回写。
- 可把结果固定为页面侧栏；翻译进行中固定时会无缝保留流式内容与分段进度，固定后新选区会自动翻译。侧栏顶部的“自动翻译中”可在当前标签页临时暂停或继续：暂停不会关闭侧栏、清除结果或中断已经开始的翻译，普通划选可继续用于复制，框选网页、右键菜单和快捷键等主动操作仍可使用；恢复后只处理下一次新选区。普通网页和 Overleaf 可在“浮动在网页上”与 Edge 浏览器侧栏之间切换：完成结果的主按钮会遵循设置中的显示方式，更多菜单保留临时改用另一种侧栏的入口；切换时直接沿用当前结果、不重复调用 API，临时暂停状态也会跟随当前标签页。浏览器侧栏的“译为”可临时切换当前标签页的中文、英文、日语、德语或法语并立即重译当前内容，后续划选继续使用该语言，不会改动其他标签页或全局默认值。浏览器侧栏会与网页并排而不遮挡正文，可用标题栏下方保持可达的轻量箭头回看当前标签页最近译文，在可靠分句存在时本地切换全文与逐句对照；长结果会按译文分别记住阅读位置并按需显示顶部、底部导航，新划词则回到新译文开头。浮动侧栏仍可收起、调整宽度和切换左右位置。
- 当前标签页最近 5、10 或 20 条翻译可通过卡片顶部的小按钮上下切换。
- 相同选区和设置可命中会话缓存，也可强制忽略缓存重新翻译。长文本某一段失败后，重试会从首个未完成分段继续，已完成译文仅保留在当前浏览器会话。
- 固定侧栏后提供一个轻量“本文”入口：按文档隔离最近译句，模型最多建议 3 个技术术语但不会自动采用；用户确认或手动添加的固定译法会优先于全局术语表。后续翻译只引用与当前选区相关的少量既有译句，并受严格上下文预算限制；术语可编辑、删除，本文记忆可一键清空。详细内容默认收起，不增加翻译卡片的常驻信息密度。
- 翻译结果可用一个小型荧光笔按钮进行“轻标记”：完整译文可标记整个选区，逐句对照可只标记某一句；原文只显示低透明度底色，不绘制下划线。悬停可阅读对应译文、滚动长译文或直接取消标记，点击原文标记可重新打开翻译卡片；更多菜单可把当前页面的标记笔记复制为包含原文、译文和来源信息的 Markdown。Pi PDF 的更多菜单还提供轻量“本文标记”导航器，按页码列出原文与译文，支持返回原句、复制全部、复制或删除单条；原文位置变化时会明确提示，不会把相似文字误当作原位置。标记不会改写网页或 Overleaf 的正文 DOM，默认只存在于当前页面会话。Pi PDF 可从更多菜单为当前文档单独开启本机保存，下次打开同一 PDF 时按匿名文档标识恢复；文字标记只有在页码、原文和前后文可靠匹配时才重新着色，框选标记使用页码与相对区域跟随缩放和侧栏布局。每篇最多保存 100 条、最多保留最近 30 篇 PDF，可随时停止保存、逐条删除或清除本文标记；普通网页和 Edge 原生 PDF 不提供持久标记。
- 普通文本在兼容接口上可流式显示；流式截断或异常终止不会保存为成功结果。只有明确的限流或服务端错误最多自动重试一次，超时和不确定的网络中断不会盲目重放请求。
- 长文本自动按句子或段落安全拆分，侧栏显示分段进度；新选区会取消已经过时的请求。PDF 文字在本地移除软连字符并保留可见硬连字符；没有可靠排版几何信息时保留原始换行，不猜测或重排双栏顺序。
- 按 API 域名与模型在会话内记住流式、JSON 模式、思考控制和视觉能力；明确不支持时只降级一次，网络、超时或配额错误不会污染记忆。
- 可选“所在句子”或“所在段落”作为消歧上下文，默认关闭且上下文不会进入译文。
- 连续翻译默认跳过密码、验证码和支付字段，并可从侧栏更多菜单暂停本网站的连续翻译。
- 提供 Qwen、DeepSeek、OpenRouter 和本机 Ollama 接口预设，同时保留完全自定义配置；文字翻译和 PDF 视觉翻译可以选择不同的 API 配置。编辑或接入第二个视觉配置不会替换当前文字翻译配置，每个配置会以紧凑状态行标明用途，也可由用户显式切换文字翻译配置。
- 新安装时通过三步向导选择服务商、填写 API Key、授权对应 API 域名并读取可用模型；设置页可随时重新运行向导。
- API 配置修复使用精确的配置修订确认：所有设置和 Key 写入完成、后台完成旧请求与检查点失效后才返回原页面续译，不依赖固定等待时间；配置随后又被修改时不会使用过期设置自动重试。
- 完整设置页使用分区导航、按需展开和固定保存栏，在桌面与小屏幕上都只显示当前任务所需内容。
- 可导出和导入不含 API Key 的安全配置文件；导入后会清除旧 Key，避免误发给新接口。
- 可复制本地诊断报告用于排查问题；报告会区分截图、排队、接口首段、接口总耗时、LaTeX 校验、结果提交、后台维护和公式渲染等阶段，但不包含 API Key、API 地址、配置名称、模型、选区、译文、页面地址、术语、截图、公式或网站名称。
- 逐句对照支持联动高亮、单句复制和本地单句修正；修正只替换对应译句，不调用翻译 API，并可随最近一次修正一起撤销。
- 翻译卡片支持拖动；`Escape` 会先取消正在进行的译文/OCR 原文修正或关闭更多菜单，再用于关闭卡片并取消进行中的请求。
- 默认建议快捷键为 `Alt+Shift+T`：有选区时翻译，无选区或页面不支持注入时打开设置页。Edge 因系统或其他扩展冲突未能分配该按键时，设置页会显示实际状态并提供快捷键管理入口。
- 使用 Pi Translator 与 P&I Lab 官方 Logo；网页浮层根据页面实际明暗主题自动适配。
- 不自动替换网页或 Overleaf 原文。

## 加载扩展

开发和构建使用 Node.js 24 LTS；仓库根目录的 `.node-version` 是 CI 与本地版本基准。

```powershell
npm ci
npm run build:edge
```

然后在 Edge 中：

1. 打开 `edge://extensions`。
2. 开启“开发人员模式”。
3. 点击“加载解压缩的扩展”。
4. 选择 `.output/edge-mv3`。

代码更新后需要在扩展管理页点击“重新加载”，并刷新已经打开的网页。

## 普通网页权限

默认的“按需翻译”模式只在用户点击右键菜单或按快捷键时，通过 `activeTab` 临时访问当前页面。

“指定网站”和“所有网站”模式用于持续显示划词按钮，保存设置时 Edge 会显示相应的网站权限请求。切换回“按需翻译”会停止自动注入；已经授予的网站权限可在 Edge 扩展详情页中手动管理。

浏览器内部页面、扩展商店及其他禁止脚本注入的页面无法翻译。

## PDF 阅读器

直接使用 Edge 原生 PDF 阅读器时，选中文字并右键选择“使用 Pi Translator 翻译选中文本”，译文会在只属于当前 PDF 标签页和当前 PDF 文档的 Edge 侧边栏中流式显示；完成后同样可直接修正自然语言部分并撤销最近一次修正。切换到其他网页时侧边栏不会跟随显示，在同一标签页打开另一地址时会清除旧会话。点击侧边栏中的“用 Pi 打开”后，Pi PDF 会继承当前在线或本地 PDF，并自动收起 Edge 侧边栏，避免出现两套翻译界面。

点击扩展工具栏中的“打开 PDF 阅读器”：

1. 当前标签页是在线 PDF 或 Overleaf 项目时，可授权其所在网站并直接继承当前 PDF；Overleaf 只暴露临时 `blob:` 预览时会提示先下载当前编译 PDF。
2. 当前标签页是本地 PDF 时，可在扩展详情页开启“允许访问文件 URL”后直接继承；未授权时仍可手动选择文件。
3. 也可以随时在 Pi PDF 中选择已经下载到本机的 PDF 文件。
4. 有文字层时直接使用原生划词；扫描版可在轻提示中点击“识别本页”生成近整页选框。使用官方 Qwen / 阿里云百炼配置时，确认后会通过同一 Key 调用专用 `qwen3.5-ocr`，把官方返回的逐行坐标转换为当前标签页的临时可选文字层。其他接口仍可使用小型“框选翻译”入口完成局部识别与翻译，但不会用不可靠坐标伪造文字层。两种选框都可在确认前移动和缩放；键盘用户可聚焦框选按钮后按 Enter 创建选框，并用方向键移动、Shift + 方向键缩放。

本地 PDF 的完整文件只在当前扩展标签页中解析，不会上传给 P&I Lab 或翻译 API。文字划选时只发送用户主动触发的文字；图像区域翻译或扫描页 OCR 只在用户调整选框并确认后发送选定区域，不发送选框外页面或完整 PDF。截图最长边会缩放到 2048 像素以内、大小限制约 3 MB，不写入历史、缓存、设置或诊断记录；OCR 文字和坐标同样只保留在当前标签页内存中。PDF 中的外部链接不会自动访问，只有用户明确点击后才会在新标签页打开。

## 开发与验证

```powershell
npm run typecheck
npm test
npm run check:secrets
npm run test:e2e
npm run build:edge
npm run zip:edge
npm run check:release
```

`npm run check:release` 会重新打包并核对版本元数据、Manifest、ZIP 文件清单与逐文件内容，同时输出包大小和 SHA-256。GitHub Actions 会在每次提交和拉取请求中自动执行密钥扫描、类型检查、单元测试、真实 Edge 端到端测试、构建、打包与一致性检查。宣传截图生成使用独立的 `npm run test:marketing`，不会混入功能回归测试。

详细设计见 [Edge MVP 设计文档](docs/edge-translation-extension-design.md)、[v0.5 历史记录、逐句对照与兼容 API 设计](docs/v0.5-history-alignment-api-design.md)、[v0.6 连续翻译侧栏与稳定性设计](docs/v0.6-continuous-sidebar-design.md)、[v0.7 稳定性、上下文与收藏设计](docs/v0.7-stability-context-favorites-design.md)、[v0.7.1 首次向导、支持工具与 CI 设计](docs/v0.7.1-onboarding-support-ci-design.md) 和 [v0.7.2 设置中心设计](docs/v0.7.2-settings-center-design.md)。发布前请查看 [更新日志](CHANGELOG.md)、[自动化验证报告](docs/verification-report.md) 和 [发布验收清单](docs/release-checklist.md)。

## 隐私与密钥

只有用户主动触发翻译的选中文本、用户确认发送的 PDF 局部截图和已配置的相关翻译约束会发送到用户指定的 API。API Key 不会注入网页，也不会打包进源码。请不要把真实 API Key 提交到 Git 仓库。

完整说明见 [隐私政策](PRIVACY.md)。上架材料见 [Edge Add-ons 提交材料](docs/edge-store-submission.md)。
