# Pi Translator

Pi Translator 是由 P&I Lab 开发的 Microsoft Edge Manifest V3 扩展，用于翻译 Overleaf、PDF 或普通网页中选中的文本。它支持用户自备的 OpenAI 兼容 API、LaTeX 结构保护、浮动翻译卡片、右键菜单和快捷键。

## 当前功能

- Overleaf 划词后显示翻译按钮。
- 内置轻量 PDF 阅读器，支持本地 PDF 和在线 PDF 的文字划选、跨行框选与流式翻译。
- Pi PDF 使用同一个框选入口智能选择处理方式：框内存在可靠文字层时在本地提取文字并走文字翻译，只有扫描版、乱码或文字覆盖不足时才把确认的局部截图发送给视觉模型。选框可移动、四角可缩放，识别或提取原文默认折叠显示并可复制或修正后仅重译；结果中的页码可返回并短暂高亮原区域，菜单可重新识别或恢复原选框继续调整。
- PDF 框选翻译支持当前浏览器会话内去重：相同截图、模型和翻译设置可复用最近结果，“重新识别”会明确绕过缓存。缓存只保存截图哈希、识别原文和译文，不保存截图、Data URL 或 Base64 图片。
- 保留 Edge 原生 PDF 阅读器：选中文字后右键翻译，结果会在 Edge 侧边栏流式显示；侧边栏仅提示可按需用 Pi PDF 打开，不会自动跳转或替换原生阅读器。
- PDF 页面按视口附近懒加载，只保留当前页附近 5 页的 Canvas 与文字层，并限制单页画布像素；支持紧凑页码跳转、适合宽度和保持页内位置的缩放。鼠标位于 PDF 文档上时，`Ctrl + 滚轮`或触控板捏合只缩放 PDF，不缩放整张阅读器页面。Pi PDF 会按匿名文档标识在本机记住最近 30 份文档的页码、页内位置、缩放和侧栏状态，不保存原始网址、文件名或正文。扫描版缺少文字层时只显示一次轻量提示，并提供“识别本页”快捷入口：它先生成可调整的近整页选框，仍需用户确认才会发送；官方 Qwen 接口可调用专用 OCR 生成仅限当前标签页的临时可选文字层，缩放后自动重建，换 PDF 后清除。
- 普通网页右键或快捷键按需翻译，默认不申请所有网站的长期权限。
- 可选择仅 Overleaf、普通网页按需、指定网站自动启用或所有网站自动启用。
- 文字翻译 API 按“服务商 → API Key → 自动读取模型”完成连接；网页、Overleaf 和可选文字 PDF 只需这一项。扫描件框选需要支持图片输入的模型，“识别本页”需要官方 Qwen / 阿里云百炼；设置页提供按需的 Qwen 入口，自定义 API 仍可填写 Base URL 和模型名称。
- 最多保存 6 组 API 配置，并从工具栏快速切换。
- 使用当前 API Key 自动读取接口可用模型。
- 可用一张不含用户内容的内置小图单独测试视觉模型能力，并区分权限、Key、余额、模型和网络错误。
- 兼容性诊断会检查模型列表、鉴权、对话接口、结构化输出和逐句能力。
- 快捷面板用一条低干扰状态行分别显示文字翻译与 PDF 图像配置状态；只检查本机配置、Key 是否存在、域名权限和已有能力记忆，不会为了显示状态自动调用 API。异常项可直接进入对应的 Key、模型或授权设置，PDF 图像未配置时普通文字翻译仍可正常使用。
- 源语言、目标语言、翻译风格和内容模式设置。
- 工具栏快速面板可切换目标语言，并临时暂停当前网站的自动划词。
- 普通网页高置信度判断选区已是目标语言时，可自动隐藏浮动按钮。
- 自动、纯文本和 LaTeX 三种内容模式。
- 译文中的行内和独立 LaTeX 公式可在输出完成后自动渲染；只有检测到公式时才显示小型“源码/公式”切换按钮，复制操作始终保留可编辑的 LaTeX 源码。渲染器随扩展本地打包，不加载 CDN 或远程代码，渲染失败时自动回退源码。
- API Key 默认仅保存到浏览器会话，也可选择本机持久保存。
- 复制译文和重新翻译。
- 完整译文与逐句对照即时切换；逐句原文来自本地选区，不由模型回写。
- 可把结果固定为页面侧栏；翻译进行中固定时会无缝保留流式内容与分段进度，固定后新选区会自动翻译，侧栏可收起、调整宽度和切换左右位置。
- 当前标签页最近 5、10 或 20 条翻译可通过卡片顶部的小按钮上下切换。
- 相同选区和设置可命中会话缓存，也可强制忽略缓存重新翻译。长文本某一段失败后，重试会从首个未完成分段继续，已完成译文仅保留在当前浏览器会话。
- 固定侧栏后提供一个轻量“本文”入口：按文档隔离最近译句，模型最多建议 3 个技术术语但不会自动采用；用户确认或手动添加的固定译法会优先于全局术语表。后续翻译只引用与当前选区相关的少量既有译句，并受严格上下文预算限制；术语可编辑、删除，本文记忆可一键清空。详细内容默认收起，不增加翻译卡片的常驻信息密度。
- 翻译结果可用一个小型荧光笔按钮进行“轻标记”：完整译文可标记整个选区，逐句对照可只标记某一句；原文只显示低透明度底色，不绘制下划线。悬停可阅读对应译文、滚动长译文或直接取消标记，点击原文标记可重新打开翻译卡片；更多菜单可把当前页面的标记笔记复制为包含原文、译文和来源信息的 Markdown。Pi PDF 的更多菜单还提供轻量“本文标记”导航器，按页码列出原文与译文，支持返回原句、复制全部、复制或删除单条；原文位置变化时会明确提示，不会把相似文字误当作原位置。标记不会改写网页或 Overleaf 的正文 DOM，默认只存在于当前页面会话。Pi PDF 可从更多菜单为当前文档单独开启本机保存，下次打开同一 PDF 时按匿名文档标识恢复；文字标记只有在页码、原文和前后文可靠匹配时才重新着色，框选标记使用页码与相对区域跟随缩放和侧栏布局。每篇最多保存 100 条、最多保留最近 30 篇 PDF，可随时停止保存、逐条删除或清除本文标记；普通网页和 Edge 原生 PDF 不提供持久标记。
- 普通文本在兼容接口上可流式显示；流式截断或异常终止不会保存为成功结果。只有明确的限流或服务端错误最多自动重试一次，超时和不确定的网络中断不会盲目重放请求。
- 长文本自动按句子或段落安全拆分，侧栏显示分段进度；新选区会取消已经过时的请求。PDF 文字在本地移除软连字符并保留可见硬连字符；没有可靠排版几何信息时保留原始换行，不猜测或重排双栏顺序。
- 按 API 域名与模型在会话内记住流式、JSON 模式、思考控制和视觉能力；明确不支持时只降级一次，网络、超时或配额错误不会污染记忆。
- 可选“所在句子”或“所在段落”作为消歧上下文，默认关闭且上下文不会进入译文。
- 连续翻译默认跳过密码、验证码和支付字段，并可从侧栏更多菜单暂停本网站的连续翻译。
- 提供 Qwen、DeepSeek、OpenRouter 和本机 Ollama 接口预设，同时保留完全自定义配置；文字翻译和 PDF 视觉翻译可以选择不同的 API 配置。编辑或接入第二个视觉配置不会替换当前文字翻译配置，每个配置会以紧凑状态行标明用途，也可由用户显式切换文字翻译配置。
- 新安装时通过三步向导选择服务商、填写 API Key、授权对应 API 域名并读取可用模型；设置页可随时重新运行向导。
- API 配置修复使用精确的配置修订确认：所有设置和 Key 写入完成、后台完成旧请求与检查点失效后才返回原页面续译，不依赖固定等待时间；配置随后又被修改时不会使用过期设置自动重试。
- 完整设置页使用分区导航、按需展开和固定保存栏，在桌面与小屏幕上都只显示当前任务所需内容。
- 可导出和导入不含 API Key 的安全配置文件；导入后会清除旧 Key，避免误发给新接口。
- 可复制本地诊断报告用于排查问题；报告不包含 API Key、选区、译文、页面地址、术语或网站名称。
- 逐句对照支持联动高亮、单句复制和单句重新翻译。
- 翻译卡片支持拖动，`Escape` 可关闭并取消进行中的请求。
- `Alt+Shift+T` 有选区时翻译，无选区或页面不支持注入时打开设置页。
- 使用 Pi Translator 与 P&I Lab 官方 Logo；网页浮层根据页面实际明暗主题自动适配。
- 不自动替换网页或 Overleaf 原文。

## 加载扩展

```powershell
npm install
npm run build:edge
```

然后在 Edge 中：

1. 打开 `edge://extensions`。
2. 开启“开发人员模式”。
3. 点击“加载解压缩的扩展”。
4. 选择 `.output/edge-mv3`。

代码更新后需要在扩展管理页点击“重新加载”，并刷新已经打开的网页。

## 普通网页权限

默认的“按需翻译”模式只在用户点击右键菜单或按快捷键时，通过 `activeTab` 临时访问当前页面。

“指定网站”和“所有网站”模式用于持续显示划词按钮，保存设置时 Edge 会显示相应的网站权限请求。切换回“按需翻译”会停止自动注入；已经授予的网站权限可在 Edge 扩展详情页中手动管理。

浏览器内部页面、扩展商店及其他禁止脚本注入的页面无法翻译。

## PDF 阅读器

直接使用 Edge 原生 PDF 阅读器时，选中文字并右键选择“使用 Pi Translator 翻译选中文本”，译文会在只属于当前 PDF 标签页和当前 PDF 文档的 Edge 侧边栏中流式显示；切换到其他网页时不会跟随显示，在同一标签页打开另一地址时会清除旧会话。点击侧边栏中的“用 Pi 打开”后，Pi PDF 会继承当前在线或本地 PDF，并自动收起 Edge 侧边栏，避免出现两套翻译界面。

点击扩展工具栏中的“打开 PDF 阅读器”：

1. 当前标签页是在线 PDF 或 Overleaf 项目时，可授权其所在网站并直接继承当前 PDF；Overleaf 只暴露临时 `blob:` 预览时会提示先下载当前编译 PDF。
2. 当前标签页是本地 PDF 时，可在扩展详情页开启“允许访问文件 URL”后直接继承；未授权时仍可手动选择文件。
3. 也可以随时在 Pi PDF 中选择已经下载到本机的 PDF 文件。
4. 有文字层时直接使用原生划词；扫描版可在轻提示中点击“识别本页”生成近整页选框。使用官方 Qwen / 阿里云百炼配置时，确认后会通过同一 Key 调用专用 `qwen3.5-ocr`，把官方返回的逐行坐标转换为当前标签页的临时可选文字层。其他接口仍可使用小型“框选翻译”入口完成局部识别与翻译，但不会用不可靠坐标伪造文字层。两种选框都可在确认前移动和缩放；键盘用户可聚焦框选按钮后按 Enter 创建选框，并用方向键移动、Shift + 方向键缩放。

本地 PDF 的完整文件只在当前扩展标签页中解析，不会上传给 P&I Lab 或翻译 API。文字划选时只发送用户主动触发的文字；图像区域翻译或扫描页 OCR 只在用户调整选框并确认后发送选定区域，不发送选框外页面或完整 PDF。截图最长边会缩放到 2048 像素以内、大小限制约 3 MB，不写入历史、缓存、设置或诊断记录；OCR 文字和坐标同样只保留在当前标签页内存中。

## 开发与验证

```powershell
npm run typecheck
npm test
npm run check:secrets
npm run test:e2e
npm run build:edge
npm run zip:edge
```

GitHub Actions 会在每次提交和拉取请求中自动执行密钥扫描、类型检查、单元测试、真实 Edge 端到端测试、构建与打包。宣传截图生成使用独立的 `npm run test:marketing`，不会混入功能回归测试。

详细设计见 [Edge MVP 设计文档](docs/edge-translation-extension-design.md)、[v0.5 历史记录、逐句对照与兼容 API 设计](docs/v0.5-history-alignment-api-design.md)、[v0.6 连续翻译侧栏与稳定性设计](docs/v0.6-continuous-sidebar-design.md)、[v0.7 稳定性、上下文与收藏设计](docs/v0.7-stability-context-favorites-design.md)、[v0.7.1 首次向导、支持工具与 CI 设计](docs/v0.7.1-onboarding-support-ci-design.md) 和 [v0.7.2 设置中心设计](docs/v0.7.2-settings-center-design.md)。发布前请查看 [更新日志](CHANGELOG.md)、[自动化验证报告](docs/verification-report.md) 和 [v0.8.0 五分钟验收清单](docs/release-checklist-v0.8.0.md)。

## 隐私与密钥

只有用户主动触发翻译的选中文本、用户确认发送的 PDF 局部截图和已配置的相关翻译约束会发送到用户指定的 API。API Key 不会注入网页，也不会打包进源码。请不要把真实 API Key 提交到 Git 仓库。

完整说明见 [隐私政策](PRIVACY.md)。上架材料见 [Edge Add-ons 提交材料](docs/edge-store-submission.md)。
