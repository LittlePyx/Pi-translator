# Pi Translator 新会话交接 Prompt

将下面整段复制到新的 Codex 任务中：

```text
继续维护 Pi Translator。项目位于 F:\research-papers\2026\July\Pi_translate，当前分支 main，v0.14.0 候选基线提交为 55e247a（docs: prepare v0.14.0 release candidate），其后可以只有交接文档提交；package 版本为 0.14.0。请先完整阅读 docs/development-handoff-v0.14.0.md，再检查 git status、最近提交、版本和候选包，不要直接开始新增功能。

当前 v0.14.0 候选包为 .output/tex-selection-translator-0.14.0-edge.zip，大小 1,149,285 bytes，SHA-256 为 2DA0D5892478EB7EAA97D91FFF2EF93D4F4EC1471D7A425CAAF860018707AD64。已通过 TypeScript、666/666 单元测试、124/124 Edge E2E、生产构建、密钥扫描和发布包逐文件一致性检查。尚未创建 v0.14.0 tag、GitHub Release 或 Edge 商店提交，也不要自行推送或发布。

当前优先事项不是新增功能，而是协助完成最终 ZIP 的真实 Edge 实机验收，重点覆盖：普通网页划词；网页框选渲染公式并确认走多模态；超过 100 段的网页零请求确认、范围预设和全文完成；PDF 全文预览、页码范围、暂停/继续、返回原文和导出；扫描 PDF 不自动 OCR、只有明确启动后才发送图像。先按 docs/release-checklist.md 核对。如果发现可复现的阻塞问题，修复后运行相应测试并重新生成候选包；如果没有问题，只报告发布准备状态和下一步，不要为了继续开发而增加低价值功能。

必须保留这些产品规则：普通网页自动划词要过滤高置信度目标语言和纯代码以免打扰；固定侧栏表示用户明确翻译意图，混合自然语言和代码仍要翻译；普通划词使用文字接口，用户主动框选网页始终使用多模态接口；网页截图、扫描页 OCR 和视觉识别必须由用户确认；全文预览、范围调整、取消和显示模式切换零请求；公式、代码、表单和未成功段落不能在只看译文时消失；不自动替换原文；不引入 Pi 账号或自有后端。

工作区里 store-assets/ads/ 下有用户的未跟踪广告素材。不要删除、移动、修改或加入代码提交。只提交本任务明确涉及的文件。若 HEAD、候选包哈希或工作区状态与交接不一致，先查清原因并向我说明，不要重置或覆盖现有内容。
```
