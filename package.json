{
  "name": "tex-selection-translator",
  "version": "0.9.0",
  "private": true,
  "description": "Microsoft Edge extension for translating selected text on Overleaf, PDF documents, and ordinary web pages.",
  "type": "module",
  "scripts": {
    "dev": "wxt -b edge",
    "prepare": "wxt prepare",
    "build:edge": "wxt build -b edge",
    "zip:edge": "wxt zip -b edge",
    "typecheck": "wxt prepare && tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "npm run build:edge && playwright test tests/e2e/selection-overlay.spec.ts tests/e2e/release-upgrade.spec.ts tests/e2e/options-api-setup.spec.ts tests/e2e/pdf-wheel-zoom.spec.ts",
    "test:live-formula": "vite-node tools/live-formula-acceptance.ts",
    "test:marketing": "npm run build:edge && playwright test tests/e2e/marketing-screenshots.spec.ts",
    "check:secrets": "node tools/check-secrets.mjs",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^20.17.30",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wxt": "^0.20.27"
  },
  "dependencies": {
    "katex": "^0.18.1",
    "pdfjs-dist": "5.4.296"
  }
}
