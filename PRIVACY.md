# Pi Translator 隐私政策

更新日期：2026 年 8 月 7 日

Pi Translator 是由 P&I Lab 开发的 Microsoft Edge 扩展。本政策说明 Pi Translator 如何处理用户数据。

## 1. 扩展的单一用途

Pi Translator 用于翻译用户在 Overleaf、PDF 阅读器或普通网页中主动选中的文本，并可在翻译过程中保护 LaTeX 结构。用户自行配置采用 OpenAI 兼容协议的翻译 API。

## 2. 处理的数据

Pi Translator 仅在用户点击浮动按钮、右键翻译菜单、使用快捷键，或主动固定连续翻译侧栏后完成新选区时处理选中文本。侧栏关闭后不会继续自动触发翻译。

- **选中的文本**：发送到用户在设置页指定的 API，以生成完整译文和可选的逐句对照结果。PDF 选区会先在本机移除软连字符并保留可见硬连字符；没有可靠排版几何信息时保留原始换行，扩展不会猜测或重排双栏顺序。
- **PDF 文件与用户确认的图像区域**：本地 PDF 的完整文件只在当前扩展标签页中解析，不发送给 P&I Lab 或用户配置的 API；在线 PDF 由扩展直接从原网站读取。文字翻译只发送用户主动选中并触发的 PDF 文字。框选翻译会先在本机检查选区内已有的 PDF 文字层；文字可靠时只发送提取文字，不发送图片。只有扫描版、乱码或文字覆盖不足，并且用户主动开启框选、调整范围并点击确认后，选定区域截图才会发送到用户选择的视觉 API。扫描页“识别本页”也会先显示可调整的近整页选框，只有用户点击“识别文字”后才把选定区域发送到官方 Qwen OCR；扩展不会发送选框外页面、其他页面或完整 PDF。
- **扫描 PDF 临时文字层**：使用官方 Qwen / 阿里云百炼 API 配置时，“识别本页”会复用该配置的 API Key 并调用专用 `qwen3.5-ocr` 高精识别任务。扩展只接受服务端原生返回的逐行文字与坐标并进行边界、数量、顺序和长度校验，不会接受通用模型自行生成的坐标。识别文字与坐标只保留在当前 Pi PDF 标签页内存中，用于浏览器划选；缩放时可重建，换 PDF、刷新或关闭标签页后清除，不写入历史、缓存、设置、配置导出或诊断报告。
- **Edge 原生 PDF 侧边栏**：用户在 Edge 原生 PDF 阅读器中右键触发翻译时，选中文字会在扩展后台完成翻译，并仅在当前浏览器会话的 Pi Translator 侧边栏中显示。为在后台进程重启或标签页切换后恢复该侧边栏，当前选中文字、部分或最终译文、PDF 地址及请求状态会保存在 Microsoft Edge 的浏览器会话存储中，并在对应标签页或浏览器关闭后清除。扩展不会读取或上传完整 PDF，也不会自动把 PDF 切换到 Pi 阅读器。
- **用户配置的翻译约束**：如果用户填写了术语映射，这些映射会随翻译请求发送到所配置的 API，以保持翻译一致。
- **可选上下文**：只有用户在“更多结果设置”中明确启用时，扩展才会额外发送选区所在句子或段落（最多 2,000 个字符）用于消歧。上下文不会作为译文展示，默认关闭。
- **API Key**：仅用于向用户指定的 API 发起鉴权请求；多组 API 配置的 Key 相互独立。
- **扩展设置**：包括 API 配置名称、API Base URL、语言、模型、翻译风格、侧栏偏好和网站范围，保存在用户本机的 Microsoft Edge 扩展存储中。
- **最近翻译**：用户启用该功能时，当前标签页最近 5、10 或 20 条原文和译文仅保存在浏览器会话存储中。
- **本文翻译记忆与固定译法**：扩展会按文档隔离，在本机扩展存储中保留最近使用的最多 40 篇文档；每篇最多保存 20 条用户主动翻译的原文与译文、100 条用户确认或手动添加的固定译法，以及 20 条待用户确认的术语候选。文档键由来源信息在本机计算为匿名哈希，不保存完整页面 URL；持久标签只使用网站域名或泛化的“PDF 文档”，不保存本地 PDF 文件名。后续翻译只会选取与当前选区有词汇交集的少量既有译句作为参考上下文，并把用户确认的本文固定译法作为翻译约束发送给用户配置的 API。模型建议的术语不会自动成为约束，只有用户点击“采用”后才会生效。用户可在侧栏“本文”中逐条编辑、删除或清空当前文档的全部记忆。
- **重复翻译缓存**：用户启用会话缓存时，相同文本及对应翻译设置的结果最多缓存 20 条，并按标签页保存在浏览器会话存储中。
- **长文本续翻检查点**：长文本分段翻译失败时，已完成分段的原文、译文、警告和可选对齐信息仅按标签页保存在浏览器会话存储中，用于重试时从首个未完成分段继续。它不包含 API Key 或图片。
- **设置修复票据**：API Key、模型、域名权限或视觉配置导致翻译中断时，用户可以从错误界面进入对应设置。扩展会在浏览器会话存储中创建一个最长保留三十分钟、只能由一个设置页领取且只能完成一次的不透明票据，用于返回原标签页。票据只包含随机标识、标签页/框架路由、错误类别和是否已有部分输出等状态；不包含 API Key、选区、译文、页面 URL、PDF 正文或截图。普通网页与 Pi PDF 的原选区或截图仅继续留在来源页面内存中，刷新、导航或关闭页面后不会从票据恢复；Edge 原生 PDF 则继续使用上文所述的当前会话侧边栏记录，票据只通过标签页和失败请求标识引用它，不复制其中正文。已有部分译文时，扩展不会在设置完成后自动再次请求 API，而是返回来源页由用户确认。
- **配置修订确认**：设置或 API Key 写入完成后，扩展会在本机保存一个随机修订标识、提交时间和是否需要清除旧翻译状态的布尔值，并在浏览器会话中记录已处理的修订标识，用于避免后台进程延迟或重启时使用旧配置恢复请求。修订记录不包含 API Key、接口地址、模型、选区、译文、页面地址或截图。
- **API 能力记忆**：扩展在当前浏览器会话内最多保留 64 条 API 域名、模型名称及其流式、JSON、思考控制和视觉兼容性布尔值，用于避免重复失败的兼容性请求。该记忆不包含 API Key、选区、译文或页面地址。
- **图像区域翻译结果**：可按最近翻译规则保存模型识别出的原文和译文；局部截图、Data URL、Blob 和图片 Base64 不进入最近翻译、重复翻译缓存、设置、配置导出或诊断摘要。
- **图像区域会话缓存**：用户启用会话缓存时，相同截图、模型和翻译设置最多缓存 20 条结果。缓存键是在本机计算的截图 SHA-256 哈希；缓存只包含哈希、识别原文、译文及翻译元数据，不包含截图、Data URL、Blob 或 Base64 图片。“重新识别”会绕过缓存。
- **PDF 结果定位**：框选结果可在当前标签页的会话历史中附带页码和相对选框坐标，用于返回原区域并短暂高亮；坐标不包含 PDF 正文或截图，并在关闭标签页或 Microsoft Edge 后清除。
- **轻标记、本文导航与笔记复制**：用户主动点击荧光笔按钮后，扩展在当前页面的内存中保留整段或单句的原文、对应译文及其网页选区引用，或 Pi PDF 的页码和相对区域坐标，用于绘制浅色标记并在悬停时显示译文。默认情况下标记不会写入扩展存储，刷新、导航、关闭标签页或重新加载扩展后自动清除。只有用户在 Pi PDF 的更多菜单中主动开启“保存本文标记”后，当前 PDF 的标记原文、译文、来源标题、页码，以及用于可靠恢复的少量前后文或相对区域坐标才会保存到本机扩展存储；每篇最多 100 条，最多保留最近 30 篇 PDF。存储键使用文档来源信息在本机计算的匿名哈希，不保存原始 PDF URL；本地 PDF 的来源标题可能包含文件名。重新打开时，文字标记只有在对应页内原文和前后文唯一匹配时才绘制，无法可靠匹配的记录不会错误着色。“本文标记”导航器仅在本机按页码整理这些已有记录，支持定位、复制和删除单条；位置变化的记录仍可复制或删除，但不会错误着色。用户可以停止后续保存、逐条删除或清除本文标记。标记与导航器不会向网页正文插入标签，也不会产生额外网络请求。只有用户主动点击复制按钮时，扩展才会把标记原文、译文及页面标题与网址，或 PDF 文件名与页码，整理为 Markdown 并写入系统剪贴板。普通网页和 Edge 原生 PDF 不提供持久标记，Edge 原生 PDF 页面也不绘制轻标记。
- **PDF 阅读状态**：Pi PDF 可在本机持久存储最近 30 份文档的匿名哈希标识、页码、页内位置、缩放、适合宽度状态和侧栏展开状态，以便下次继续阅读。匿名标识由文档来源信息在本机单向计算，不保存原始 PDF 地址、文件名、正文或截图。
- **临时暂停的网站域名**：仅保存在当前浏览器会话中。
- **本地故障摘要**：扩展在当前浏览器会话中最多保留最近 20 条失败操作的时间、操作类型、错误代码、是否可重试及可选 HTTP 状态码。摘要不保存错误正文、API Key、选区、译文、页面地址、术语或网站名称。

页面 URL 只在本机用于判断页面类型、LaTeX 处理方式和网站权限，不会作为翻译请求内容发送。Pi Translator 不会自动发送未被用户主动选中并触发翻译的网页或 PDF 正文，也不会在用户确认选框前截取或上传 PDF 页面区域。

连续翻译启用敏感字段保护时，会在本机识别密码、验证码和支付字段并跳过自动发送。该保护不分析或上传字段内容。长文本可能在本机拆分为多个请求；普通文本在接口支持时可能以流式响应接收，但发送的数据类型不因此改变。

## 3. 数据存储

- API Key 默认保存在 Microsoft Edge 的会话存储中；用户也可以明确选择保存在本机持久存储中。
- 最近翻译默认启用并按标签页隔离；用户可选择保留最近 5、10 或 20 条，并可在设置中关闭该功能。关闭功能、关闭对应标签页或关闭 Microsoft Edge 后，这些会话记录会被清除。
- 文本和图像区域的重复翻译缓存默认启用，各最多 20 条；关闭缓存、关闭标签页或关闭 Microsoft Edge 后会自动清除。图像缓存不保存截图本身。
- 长文本续翻检查点在翻译成功、用户取消、改变翻译行为设置、导航到其他页面、关闭对应标签页或关闭 Microsoft Edge 时清除。
- 设置修复票据最多同时保留 8 条，并在完成一次恢复、来源页或设置页关闭、超过三十分钟或关闭 Microsoft Edge 后失效；过期记录会在下一次恢复操作或关闭 Microsoft Edge 时物理清除。
- 当前配置修订标识保存在本机扩展存储中，新的设置提交会覆盖旧标识；已处理标识仅保存在浏览器会话存储中。两者都不包含实际配置值或用户内容。
- API 能力记忆仅保存在浏览器会话存储中；关闭 Microsoft Edge 后清除。
- Pi PDF 轻标记默认只存在于当前页面内存中。用户为某篇 PDF 主动开启“保存本文标记”后，该文档最多 100 条标记会保存在本机扩展存储中；最多保留最近 30 篇文档，并可通过“停止保存本文标记”停止更新或通过“清除本文标记”删除。普通网页与 Edge 原生 PDF 的标记不会持久保存。
- 本地故障摘要只保存在浏览器会话存储中，关闭 Microsoft Edge 后自动清除。诊断报告仅在用户主动点击后生成并复制到剪贴板，不会自动发送给 P&I Lab 或第三方。
- PDF 阅读状态最多保留 30 份并仅存于 Microsoft Edge 扩展的本地存储中；卸载扩展会删除这些状态。状态不包含 PDF 地址、文件名、正文或截图。
- 用户可主动导出设置、API 配置名称与地址、网站范围和术语映射到本机 JSON 文件。导出文件明确不包含 API Key；导入配置时扩展会清除已有 Key，并要求用户重新填写。
- 本文翻译记忆不会进入设置导出文件；用户可以在对应文档的侧栏“本文”抽屉中清空，卸载扩展也会删除这些本机记录。
- Pi Translator 不使用分析、遥测、广告或用户跟踪服务。
- P&I Lab 不运营用于接收选中文本、API Key 或翻译结果的中转服务器。

## 4. 第三方服务

翻译请求从扩展后台直接发送到用户配置的 API Base URL。该服务对请求数据的处理、保留、计费和安全措施由用户选择的 API 服务商及用户与其之间的协议决定。用户应阅读相应服务商的隐私政策，并且不应选择或发送自己无权处理的个人信息、机密信息或敏感信息。

只有用户主动点击“兼容性诊断”时，扩展才会向当前 API 发送一条固定的短测试文本 `Hello.`，用于检查对话、结构化输出和逐句能力。诊断可能产生极少量 API 用量。

只有用户主动点击“测试视觉能力”时，扩展才会向所选视觉 API 发送一张扩展内置的 130×58 高对比字符测试图，用于确认模型确实读取了图片输入。该测试图不包含 PDF、网页截图或其他用户内容，也不会写入历史或缓存；测试可能产生极少量 API 用量。

## 5. 权限与用户控制

用户可以随时：

- 不触发翻译，从而不发送任何选中文本；
- 在设置页清除任一 API 配置的 Key、删除 API 配置、关闭最近翻译或关闭会话缓存；
- 导出或导入不含 API Key 的设置文件，并复制不含翻译正文和密钥的本地诊断报告；
- 关闭“记住最近翻译”以清除会话中的最近翻译记录；
- 在固定侧栏的“本文”抽屉中编辑或删除单条固定译法，或清空当前文档的翻译记忆；
- 在快速面板中暂停本网站的自动划词；
- 在快速面板中打开内置 PDF 阅读器；
- 在 Edge 原生 PDF 阅读器中通过右键菜单打开 Pi Translator 侧边栏；
- 关闭上下文辅助、流式显示或敏感字段保护；
- 在 Microsoft Edge 的扩展详情页管理 API 域名和网站访问权限；
- 卸载扩展以移除扩展及其本机数据。

Pi Translator 只在用户保存设置、读取模型、测试或诊断接口、测试视觉能力，或在快捷面板切换 API 配置时，申请当前所需 API 域名的访问权限。普通网页默认采用按需访问；只有用户选择指定网站或所有网站自动显示划词按钮时，才会申请相应的长期网站权限。`file:///*` 是可选权限，仅在用户从 Edge 原生本地 PDF 主动选择“用 Pi 打开”时使用；用户还必须在扩展详情页单独开启“允许访问文件 URL”。

`sidePanel` 权限仅用于在用户从 Edge 原生 PDF 的右键菜单主动触发翻译后显示译文；侧边栏按标签页和 PDF 文档隔离，只在触发它的 PDF 标签页可用，不会因普通浏览行为自动打开或跟随到其他网页。同一标签页导航到其他地址时，旧 PDF 会话会被清除。

## 6. 数据保留与删除

P&I Lab 不接收或保留用户的翻译请求。第三方 API 对请求的保留和删除规则由该服务商的政策及用户账户设置决定。扩展本机保存的数据可通过上述控制项或卸载扩展删除。

## 7. 儿童

Pi Translator 不以儿童为目标用户，也不会故意收集儿童个人信息。

## 8. 政策更新与联系

功能或数据处理方式变化时，本政策会同步更新。如有隐私或支持问题，请通过 [Pi Translator GitHub Issues](https://github.com/LittlePyx/Pi-translator/issues) 联系维护者。
