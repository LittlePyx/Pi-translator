# Pi Translator 隐私政策

更新日期：2026 年 7 月 29 日

Pi Translator 是由 P&I Lab 开发的 Microsoft Edge 扩展。本政策说明 Pi Translator 如何处理用户数据。

## 1. 扩展的单一用途

Pi Translator 用于翻译用户在 Overleaf 或普通网页中主动选中的文本，并可在翻译过程中保护 LaTeX 结构。用户自行配置采用 OpenAI 兼容协议的翻译 API。

## 2. 处理的数据

Pi Translator 仅在用户点击浮动按钮、右键翻译菜单、使用快捷键，或主动固定连续翻译侧栏后完成新选区时处理选中文本。侧栏关闭后不会继续自动触发翻译。

- **选中的文本**：发送到用户在设置页指定的 API，以生成完整译文和可选的逐句对照结果。
- **用户配置的翻译约束**：如果用户填写了术语映射，这些映射会随翻译请求发送到所配置的 API，以保持翻译一致。
- **可选上下文**：只有用户在“更多结果设置”中明确启用时，扩展才会额外发送选区所在句子或段落（最多 2,000 个字符）用于消歧。上下文不会作为译文展示，默认关闭。
- **API Key**：仅用于向用户指定的 API 发起鉴权请求；多组 API 配置的 Key 相互独立。
- **扩展设置**：包括 API 配置名称、API Base URL、语言、模型、翻译风格、侧栏偏好和网站范围，保存在用户本机的 Microsoft Edge 扩展存储中。
- **最近翻译**：用户启用该功能时，当前标签页最近 5、10 或 20 条原文和译文仅保存在浏览器会话存储中。
- **重复翻译缓存**：用户启用会话缓存时，相同文本及对应翻译设置的结果最多缓存 20 条，并按标签页保存在浏览器会话存储中。
- **临时暂停的网站域名**：仅保存在当前浏览器会话中。
- **用户主动收藏的翻译**：收藏的原文、译文、来源网站域名及翻译元数据保存在本机持久存储中，最多 100 条；只有用户主动收藏时才会保存。
- **本地故障摘要**：扩展在当前浏览器会话中最多保留最近 20 条失败操作的时间、操作类型、错误代码、是否可重试及可选 HTTP 状态码。摘要不保存错误正文、API Key、选区、译文、页面地址、术语或网站名称。

页面 URL 只在本机用于判断页面类型、LaTeX 处理方式和网站权限，不会作为翻译请求内容发送。Pi Translator 不会自动读取或发送未被用户主动选中并触发翻译的网页正文。

连续翻译启用敏感字段保护时，会在本机识别密码、验证码和支付字段并跳过自动发送。该保护不分析或上传字段内容。长文本可能在本机拆分为多个请求；普通文本在接口支持时可能以流式响应接收，但发送的数据类型不因此改变。

## 3. 数据存储

- API Key 默认保存在 Microsoft Edge 的会话存储中；用户也可以明确选择保存在本机持久存储中。
- 最近翻译默认启用并按标签页隔离；用户可选择 5、10 或 20 条，也可固定、删除、导出或清空。固定只影响当前会话内的记录顺序，不会转为永久存储。
- 重复翻译缓存默认启用，最多 20 条；关闭缓存、关闭标签页或关闭 Microsoft Edge 后会自动清除。
- 用户主动收藏的翻译最多 100 条并保存在本机，用户可以在侧栏的收藏搜索界面逐条删除；卸载扩展会一并移除。
- 本地故障摘要只保存在浏览器会话存储中，关闭 Microsoft Edge 后自动清除。诊断报告仅在用户主动点击后生成并复制到剪贴板，不会自动发送给 P&I Lab 或第三方。
- 用户可主动导出设置、API 配置名称与地址、网站范围和术语映射到本机 JSON 文件。导出文件明确不包含 API Key；导入配置时扩展会清除已有 Key，并要求用户重新填写。
- Pi Translator 不使用分析、遥测、广告或用户跟踪服务。
- P&I Lab 不运营用于接收选中文本、API Key 或翻译结果的中转服务器。

## 4. 第三方服务

翻译请求从扩展后台直接发送到用户配置的 API Base URL。该服务对请求数据的处理、保留、计费和安全措施由用户选择的 API 服务商及用户与其之间的协议决定。用户应阅读相应服务商的隐私政策，并且不应选择或发送自己无权处理的个人信息、机密信息或敏感信息。

只有用户主动点击“兼容性诊断”时，扩展才会向当前 API 发送一条固定的短测试文本 `Hello.`，用于检查对话、结构化输出和逐句能力。诊断可能产生极少量 API 用量。

## 5. 权限与用户控制

用户可以随时：

- 不触发翻译，从而不发送任何选中文本；
- 在设置页清除任一 API 配置的 Key、删除 API 配置、关闭最近翻译或关闭会话缓存；
- 导出或导入不含 API Key 的设置文件，并复制不含翻译正文和密钥的本地诊断报告；
- 在翻译卡片中清空当前标签页的最近翻译；
- 在翻译卡片中收藏结果，并在收藏搜索界面逐条删除；
- 在快速面板中暂停当前网站的自动划词按钮；
- 关闭上下文辅助、流式显示或敏感字段保护；
- 在 Microsoft Edge 的扩展详情页管理 API 域名和网站访问权限；
- 卸载扩展以移除扩展及其本机数据。

Pi Translator 只在用户保存设置、读取模型或测试连接时申请所配置 API 域名的访问权限。普通网页默认采用按需访问；只有用户选择指定网站或所有网站自动显示划词按钮时，才会申请相应的长期网站权限。

## 6. 数据保留与删除

P&I Lab 不接收或保留用户的翻译请求。第三方 API 对请求的保留和删除规则由该服务商的政策及用户账户设置决定。扩展本机保存的数据可通过上述控制项或卸载扩展删除。

## 7. 儿童

Pi Translator 不以儿童为目标用户，也不会故意收集儿童个人信息。

## 8. 政策更新与联系

功能或数据处理方式变化时，本政策会同步更新。如有隐私或支持问题，请通过 [Pi Translator GitHub Issues](https://github.com/LittlePyx/Pi-translator/issues) 联系维护者。
