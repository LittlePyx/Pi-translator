# 更新日志

## 未发布

### 改进

- “译全文”现在会在一次明确启动后自动完成全部已识别正文，不再要求用户滚动到每一段；队列仍持续按当前阅读位置重排，眼前内容优先，暂停、停止、范围调整、交互翻译抢占及动态正文复用保持不变。运行状态改为“全文翻译中”，避免把用户误导为必须继续滚动。
- 网页全文翻译改为自适应批量请求：当前视口使用最多 2 段的小批次快速回填，后台正文最多合并 6 段，模型按不可变段落 ID 分别返回译文。批次漏回、重复返回或破坏某段公式时，只把对应段降级为单段重试，成功段落不会重发；滚动重排、暂停、停止、会话恢复和划词抢占仍以批次边界安全衔接。
- Pi PDF 顶部新增直接可见的“译全文”：第一次点击只在本地读取整篇文字层并预报段数，用户确认后才批量发送；译文在不改动 PDF 画布的右侧阅读面板中按页排列，当前阅读页优先，支持暂停、继续、停止、清除、返回原页和单段重试。无文字层的扫描页保留原页并明确标记，不会为了全文任务自动截图或调用 OCR。

## 0.14.0 - 2026-08-27

### 新增

- 普通网页与 Overleaf 的快捷面板新增一次性网页区域框选，面向文字、图像、图表、Canvas、渲染公式和复杂混排。用户确认后只截取当前可见页中的框内画面，并交给多模态翻译 API；普通划词继续使用文字接口，两种入口不混用。选框支持移动、四角缩放和 `Escape` 取消，不提供整页或连续截图。
- 普通网页新增显式的正文双语阅读：从快捷面板或 Edge 浏览器侧栏开始后，保留原段落并在下方插入译文，只优先处理当前视口与下一屏，滚动后继续。正文识别会跳过导航、表单、表格、代码块和独立公式；翻译可暂停、停止后继续或一键清除，临时段落请求不会写入划词历史或覆盖侧栏译文。

### 安全与交互

- 长文章的正文翻译不再只藏在快捷面板或侧栏中：用户打开 Pi Translator 后，网页右下角会显示可关闭的轻量“译全文 · N 段”入口。默认按需模式利用这次用户操作激活当前页，不新增常驻读取权限；短页面及正文已是目标语言的页面不显示。入口和运行控制条都可打开“范围”预览，点击正文段落即可排除或恢复；预览与取消不调用接口，应用时保留并复用未排除译文，只清除或补译实际变化的段落。
- 网页框选确认界面明确提示将使用多模态模型，并说明只截取当前可见页中的框内区域；检测到密码、验证码或支付字段时始终禁止发送。快捷面板将视觉能力统一标为“图像翻译”，浏览器侧栏的历史箭头增加“最近译文”可见标签，降低窄侧栏中的导航歧义。
- Edge 浏览器侧栏在普通网页上新增粘性主操作“框选网页”：空状态和已有译文状态下始终可见，点击即可进入当前网页框选；PDF、扩展内部页及其他不支持的页面自动隐藏，并在启动失败时就地提供重试反馈。
- 网页内固定翻译侧栏在标题下方同步提供整行“框选网页”入口，空闲、翻译中和结果状态均保持可见；开始框选时侧栏临时让出页面，按 `Escape` 取消会无请求地恢复原侧栏，Pi PDF 侧栏不显示该网页操作。
- 网页框选结果和失败提示新增“调整区域 / 重新框选”：调整时恢复上一次矩形，重新框选则从空白画布开始；两种入口都必须再次确认才会请求 API。网页选框同时支持 `Enter` 建框、方向键移动、`Shift + 方向键` 缩放和 `Ctrl + 方向键` 细调。
- 正文双语阅读改为段落级失败隔离：单段过长、空结果、异常格式或 LaTeX 保护失败时，在原段落下方提供“重试本段”，并继续处理其他正文；页面控制条与 Edge 浏览器侧栏同步显示待重试段数。连续多个段落失败，以及限流、网络、鉴权和配置错误仍会暂停整篇，避免连续发送无效请求。
- 正文双语阅读与划词、右键和框选翻译不再互相误伤：用户主动翻译当前选区时，正文会显示“正在处理划词，正文稍后继续”，被替换的当前段静默回到队首；选区翻译结束后自动续跑，不会生成虚假的段落失败，也不会覆盖用户手动暂停、停止或清除的决定。
- 正文双语译文新增按段操作：鼠标悬停或键盘聚焦时可复制、隐藏/显示或仅重译当前段，平时不占用阅读空间。浏览器会话内首次译出正文时短暂提示一次；无悬停设备默认只显示“更多”，展开后才显示完整操作。单段重译会明确绕过缓存；接口或公式保护校验失败时保留原译文，不影响整页已完成进度。
- 网页右下角控制条和 Edge 浏览器侧栏新增“收起/展开全部译文”，只切换当前页显示而不重新请求接口；动态加载的新译文会继承全局收起状态，重新展开时仍保留用户对单段译文的隐藏选择。
- 正文目标语言会在快捷面板、Edge 浏览器侧栏和网页控制条中保持可见并同步。未产生译文时可直接切换；已有译文时会显示已译段数并说明将清除结果、重新调用接口，确认后才执行，取消不会清除译文或发送请求。
- 正文双语阅读可跟随无限滚动和前端动态加载：只监听已识别的正文区域，新段落接近视口后才请求翻译；React 等框架以相同文本重建节点时复用已有译文。暂停或停止期间只登记新增内容，继续后再翻译；SPA 切换为另一篇正文时清除旧译文并提示重新开始，避免把旧结果贴到新文章。
- 正文双语阅读的待发送队列会跟随当前阅读位置重排：视口内段落优先，其次处理下方即将读到的内容，已经滚过的旧队列延后；正在进行的请求不因滚动取消，用户主动重试仍优先，也不会增加接口调用次数。
- 正文段落请求超过 350ms 时会在对应原文下方显示一行低干扰“正在翻译此段…”，完成或失败后原位替换；快速请求不闪烁，排队段落不显示占位，全局收起、停止或划词和框选翻译抢占时及时清理。
- 浏览器侧栏不再把译文渲染或公式排版引起的页面滚动误判为用户主动接管；新的长译文稳定从开头显示，只有真实滚轮、触控、滚动条或键盘导航才会保留用户当前位置。
- 正文逐段翻译会参考文章标题和紧邻的上一段；上一段已经译完时，其译文也会作为参考，改善代词、短段落和上下句衔接。参考内容最多 800 字符，只用于当前请求消歧，模型仍被要求仅输出当前段；它不会产生额外请求，也不会写入翻译历史或持久化正文。
- 浮动翻译卡拖动期间由窗口级指针事件维持位移；Edge 偶发提前丢失 pointer capture 时不再只移动第一小步。

### 工程质量

- 开发与 CI 基准升级到 Node.js 24，并更新 GitHub Actions 的 Node 24 运行时版本；重复推送会自动取消同一分支的旧验证任务，端到端测试入口也由单一脚本维护。
- 商店提交材料和发布验收清单改为从 `package.json` 读取当前版本，避免安装包、审核说明和文档链接继续停留在历史版本。
- Edge 端到端测试失败时会保留并上传短期 Playwright trace，便于还原真实浏览器中的焦点、布局和时序问题。

## 0.13.0 - 2026-08-18

### 新增

- 普通网页与 Overleaf 的快捷面板新增一次性网页区域框选，面向图像、图表、Canvas、渲染公式和复杂混排。框内存在可靠 DOM 文字时默认只发送本地提取文字；只有没有可靠文字或用户明确切换时，才在确认后截取当前可见页的局部区域并交给图像翻译 API。选框支持移动、四角缩放和 `Escape` 取消，不提供整页或连续截图。

### 安全与交互

- 网页框选确认界面明确显示本次将发送文字还是截图；检测到密码、验证码或支付字段时始终禁止发送。快捷面板将视觉能力统一标为“图像翻译”，浏览器侧栏的历史箭头增加“最近译文”可见标签，降低窄侧栏中的导航歧义。
- Edge 浏览器侧栏在普通网页上新增粘性主操作“框选网页”：空状态和已有译文状态下始终可见，点击即可进入当前网页框选；PDF、扩展内部页及其他不支持的页面自动隐藏，并在启动失败时就地提供重试反馈。
- 网页内固定翻译侧栏在标题下方同步提供整行“框选网页”入口，空闲、翻译中和结果状态均保持可见；开始框选时侧栏临时让出页面，按 `Escape` 取消会无请求地恢复原侧栏，Pi PDF 侧栏不显示该网页操作。
- 网页框选结果和失败提示新增“调整区域 / 重新框选”：调整时恢复上一次矩形和文字或截图模式，重新框选则从空白画布开始；两种入口都必须再次确认才会请求 API。网页选框同时支持 `Enter` 建框、方向键移动、`Shift + 方向键` 缩放和 `Ctrl + 方向键` 细调。
- 浮动翻译卡拖动期间由窗口级指针事件维持位移；Edge 偶发提前丢失 pointer capture 时不再只移动第一小步。

### 工程质量

- 开发与 CI 基准升级到 Node.js 24，并更新 GitHub Actions 的 Node 24 运行时版本；重复推送会自动取消同一分支的旧验证任务，端到端测试入口也由单一脚本维护。
- 商店提交材料和发布验收清单改为从 `package.json` 读取当前版本，避免安装包、审核说明和文档链接继续停留在历史版本。
- Edge 端到端测试失败时会保留并上传短期 Playwright trace，便于还原真实浏览器中的焦点、布局和时序问题。

## 0.12.1 - 2026-08-17

### 新增

- 网页浏览器侧栏会复用当前标签页的最近译文，在顶部以“上一条 / 计数 / 下一条”的轻量导航回看；切换历史不会调用翻译 API，新划词开始后自动回到最新结果，数量继续服从既有的 5 / 10 / 20 条设置。
- 普通网页与 Overleaf 的连续翻译新增“浏览器侧栏”显示方式：可从现有结果直接切换到 Edge 原生侧栏，当前译文会原样迁移而不重复调用 API，后续划词继续在独立侧栏中更新；设置页和快捷面板会记住“浮动侧栏 / 浏览器侧栏”偏好，也可从原生侧栏切回网页浮层。
- 普通网页和 Overleaf 的完成结果会直接按已保存偏好显示“固定侧栏”或“打开浏览器侧栏”；更多菜单中的另一种显示方式仅用于本次临时切换，不会暗中修改偏好。浏览器拒绝打开侧栏时结果卡会保留，并在原按钮内给出可重试反馈。
- 浏览器侧栏在完整结果包含可靠分句时提供紧凑的“全文 / 逐句”切换；逐句模式按原文、译文纵向排列并沿用公式渲染，切换历史或继续划词时保留当前阅读模式，整个过程不调用翻译 API。
- 浏览器侧栏会为当前会话中的每条长译文分别保留阅读位置：切换历史后回到原处，新划词结果从译文开头显示，全文、逐句和公式源码重绘时维持阅读进度；只有内容确实超出视口时，底部才显示可用 Home / End 操作的顶部、底部导航。最近译文的箭头与计数会轻量固定在标题栏下方，读到长译文底部也可直接切换，不必先滚回页面顶部。
- Pi PDF 会从真实文字块边界识别明显的双栏间隔：浏览器意外把另一栏带入选区时自动保留起始栏，明确拖入另一栏时保留跨栏意图但不重排原文；拖选轨迹实际经过的通栏公式、标题或图注会作为中性内容保留，不会被误删或误判为另一栏。翻译按钮旁会先显示实际选区预览和相应提示，连续翻译侧栏中则始终优先限制在起始栏。
- Pi PDF 会保守识别多行、多列的表格型选区：普通模式提示划词顺序风险并可一键把拖选范围转换为可调整框选，连续翻译侧栏中不会自动发送顺序可疑的表格文字；普通双栏正文和单行并排内容不会因此被当作表格。
- 快捷面板新增轻量使用状态检查：文字翻译配置、Edge 实际快捷键或当前网页支持存在问题时才展开并给出单一步骤，全部正常时收为一行“Pi Translator 已就绪”；浏览器限制页面只作上下文提示，不误报为配置故障。
- 单词或短语等短选区可在同一次翻译请求中返回紧凑释义：优先显示当前语境译义，并按模型可靠输出补充读音、词性和最多 3 条常用释义；长句、LaTeX、手动调整及不支持该结构的接口继续使用原译文卡。
- 短词释义可切换回普通译文显示；原文朗读只采用浏览器明确标记为本地的系统语音，没有本地语音时不会调用可能联网的语音服务。

### 修复

- 设置页改为读取 Edge 实际分配的翻译快捷键，不再把清单中的建议键误报为已生效；默认键因系统或其他扩展冲突而留空时，会明确显示“翻译快捷键未设置”并提供设置入口，从快捷键页面返回后自动刷新。

## 0.12.0 - 2026-08-13

### 新增

- 翻译历史支持按原文、译文、网站和语言搜索；输入时即时筛选，没有匹配项时给出明确空状态，清空搜索即可返回完整记录。
- 最近三版译文不再只显示时间：切换版本时会用简短摘要说明相对当前版本增加、删减或调整了多少内容，便于判断是否需要恢复。
- 结果卡会列出本次实际命中的固定术语及作用范围；仅展示确实参与当前请求的术语，不增加新的网络请求。
- 本文术语可标记为“待核对”，并与公式、OCR、模型调整等事项汇总到一个轻量的“本文待核对”入口；全部处理后入口自动消失。

### 交互与界面

- 快捷面板会根据当前页面优先展示可执行操作，隐藏无关网站控制，并统一状态反馈、首次 API 设置引导、字段间距和触控目标。
- 网页卡片与 Edge PDF 侧栏对长原文、长译文和密集操作做渐进收纳；关键复制、修正、继续翻译与返回原文操作在窄面板和长内容中仍保持可达。
- 流式输出会在用户停留底部时智能跟随，主动上滚后保持阅读位置；修正、撤销、逐句对照和历史切换也尽量维持当前阅读上下文。
- Pi PDF 的工具栏、区域队列、OCR 核对、标记导航和选框手柄适配更窄视口与触控操作，并限制长内容越界。
- 设置页进一步压缩信息层级，首次连接与任务恢复只强调当前需要完成的一步。

### 工程质量

- 新增发布包一致性门禁，逐项核对版本元数据、MV3 Manifest、ZIP 路径、文件清单与文件内容，并输出 SHA-256。
- 补充历史搜索、版本摘要、术语命中、待核对汇总、窄屏布局、长内容导航、修正恢复和 PDF 交互的单元与 Edge 端到端回归测试。

## 0.11.0 - 2026-08-10

### 新增

- 翻译流在网络、超时或服务商错误后中断时，会保留已经收到的部分译文，并按错误类型提供复制、重试或定向检查设置；这些操作只在失败时出现，不增加正常结果页的常驻控件。
- API Key、模型、域名权限或视觉配置导致翻译中断时，可从结果卡片或 Edge PDF 侧栏进入对应设置；连接验证并保存成功后，扩展会返回原标签页，并在安全条件满足时继续刚才的翻译。
- 设置页新增紧凑的任务恢复提示，区分文字 API 与 PDF 图像 API，并把焦点直接放到需要处理的配置项。
- 快捷面板新增一条扁平的 API 状态行，仅显示“文字翻译”和“PDF 图像”；正常状态保持低对比，缺少 Key、模型或域名权限时才强调，并可点击直达对应设置。
- 普通网页、Overleaf、Pi PDF 与 Edge 原生 PDF 的完成结果新增轻量“修正”入口；自然语言可直接编辑，LaTeX 片段保持只读，保存修正不会重新调用翻译 API。
- 手工修正的译文保存范围精简为“仅本次”和“记住本文”；固定术语默认收起且完全可选，并可独立保存到本文或全局。术语只采用用户明确填写的原文与固定译法，不会从整句修改中自动推断。
- 逐句对照可在本地只修正目标译句，不调用翻译 API，也不会清除其余译句或对齐关系；最近一次单句修正可撤销。
- 保存后可撤销最近一次修正；若术语在修正后又被用户单独改动，撤销会保留新的术语映射，只恢复译文。

### 安全与交互

- 区分无效接口地址、不可用模型、主动取消与远端流异常；`model_not_found` 等服务商机器码会正确引导检查模型，错误 API 路径会直接引导检查 Base URL。
- 键盘操作在翻译卡片重绘、公式/源码切换、标记切换和侧栏折叠后保留在对应控件；关闭固定侧栏后会回到翻译前的页面焦点，原生 PDF 重试也不会在后续流式更新中反复抢焦点。
- 流式译文默认跟随最新内容；主动向上滚动阅读时会保留当前位置，滚回底部后自动恢复跟随，不增加额外开关或常驻控件。
- 网页浮层与 Edge PDF 侧栏统一使用易读的翻译耗时：不足一秒显示毫秒，较长请求显示一位小数秒；会话缓存不再同时显示旧请求耗时。
- 恢复任务只在浏览器会话存储中保留三十分钟的一次性不透明票据及标签页路由；API Key、选区正文、部分译文、页面 URL 与 PDF 截图均不会进入恢复票据。
- 恢复投递采用领取、投递中、确认送达三阶段；多标签 Pi PDF 不会抢占恢复消息，Edge 原生 PDF 只会恢复票据绑定的失败请求，待确认状态在侧边栏关闭后仍可找回。
- 已收到部分译文或错误不适合自动重试时，不会静默再次调用 API；返回原页面后由用户明确选择保留现有内容或重新翻译，避免意外重复计费。
- 配置类阻塞错误只显示一个明确的设置主操作；过期、页面切换或原任务变化会安全终止恢复并提示重新选择。
- 设置恢复不再依赖固定延时：设置与 API Key 写入完成后提交一个不含配置内容的随机修订号，后台确认该精确修订已应用后才继续原请求；其他页面再次修改配置时会拒绝用旧设置恢复。
- 快捷面板的 API 状态只读取本机配置、Key 是否存在、域名权限和已有会话能力记忆，不会自动发送测试请求或产生 API 用量；未配置 PDF 图像能力不会影响普通文字翻译。
- 手工修正同时在编辑器和后台验证 LaTeX 片段的数量、内容与顺序，并拒绝过期草稿、过期 PDF 会话、重复字段和未知字段，避免修正覆盖更新后的译文或破坏公式。
- 撤销只接受与当前修正结果完全匹配的一次性回执；术语回滚采用条件更新，不会覆盖用户在保存修正后另行做出的修改。
- 同一标签页的翻译结果、修正与撤销按顺序提交，并以当前结果头拒绝旧编辑器、旧译文或过期撤销覆盖新结果；Edge 原生 PDF 还会校验 PDF 会话和修正回执。“本文译文 + 本文术语”在一次本地原子写入中提交，避免部分保存。
- 结果头缺失时修正会安全失败，不再把未知状态视为可写；最终提交使用比较并交换推进结果头，提交前的本文记忆或术语变更会在失败时按相反顺序补偿。Edge 原生 PDF 的新选区、重试、修正、撤销、换文档和标签页关闭共用同一标签页提交通道，避免旧会话在导航后重新出现。
- 同 URL 刷新也会终止上一份页面文档中的翻译任务；文字与图像翻译会先提交结果头及匹配的 Edge 原生 PDF 可恢复侧栏会话，成功后才维护历史、缓存和本文记忆，存储或生命周期失败时不会留下未提交结果。本文术语撤销会恢复被确认操作移除的待确认候选，同时仍尊重用户之后做出的术语修改。
- 文字、缓存和图像翻译的最终完成态现在只在结果头及匹配 PDF 会话提交成功后显示；历史、缓存和本文记忆继续在后台维护，不再让用户在最后一段译文到达后额外等待，也不会短暂显示一个随后提交失败的结果。
- 设置写入在扩展页面之间串行化；设置页以“打开时状态、当前草稿、最新存储”三方合并，保留快捷面板、后台和其他设置页的新改动。并发新增 API 配置或术语达到容量上限时明确报错，不静默删除数据。
- API Key 的迁移、保存、清除与存储位置切换共用跨页面写锁；接口地址变化、配置删除、首次设置和配置导入采用失败关闭顺序，先隔离旧 Key 再公开新接口。相同接口只切换模型会继续保留 Key。
- 设置页持有待保存 Key 时会校验原 API 配置仍存在且接口地址未被其他页面更改；并发删除或换接口会要求重新加载，不会写入孤立 Key。本文翻译在满容量回滚时优先保留后来新增的记录，不再为了恢复旧回执挤掉用户之后的翻译。
- 术语匹配统一折叠连续空白；用户明确要求保持原拼写时允许原文与固定译法相同，术语表已满时会明确提示，而不会静默淘汰旧术语。
- `Escape` 会优先取消当前修正、OCR 原文编辑或关闭更多菜单，不再误关整张结果卡；Pi PDF 的框选快捷键也不会抢走翻译编辑器的取消操作。
- 网页浮层的译文、逐句、识别原文与标记复制统一使用原按钮内的短暂成功/失败反馈；Edge PDF 侧栏复制统一输出标准单反斜杠 LaTeX，并在窄侧栏中自动换行。

### 测试

- 增加部分流式结果失败恢复、接口与模型错误分类、远端 SSE 意外中断、原生 PDF 部分译文复制，以及翻译卡片和固定侧栏焦点连续性的单元与 Edge 端到端回归测试。
- 增加恢复票据的过期、单次领取、投递确认、同毫秒容量裁剪、标签页清理与敏感字段白名单测试；普通网页恢复采用 Edge 端到端测试，Pi PDF 定向投递与 Edge 原生 PDF 待确认持久化纳入对应回归测试。
- 增加精确配置修订、后台重启幂等、并发设置页领取、多类恢复票据隔离、原生 PDF 会话重启，以及 Pi PDF 从缺少 Key 到返回同一选区续译的回归覆盖。
- 增加手工修正分段、LaTeX 锁定、过期草稿、独立术语范围、逐句本地修正、结果头校验、条件回滚和连续撤销的单元测试，并补充紧凑入口、全局术语保存与撤销、标记同步、复制反馈、局部 `Escape`、焦点恢复和窄侧栏的 Edge 端到端覆盖。
- 增加同一基础结果的双修正竞争、较新修正后的旧撤销、结果头丢失无副作用、本文术语原子回执与条件补偿测试；完整单元测试与 Edge 端到端测试在本轮变更后重新通过。
- 增加同 URL 刷新生命周期、PDF 会话持久化失败恢复、跨扩展页面设置锁、设置草稿三方合并、术语满额冲突及本文候选术语无损撤销测试。
- 增加文字/图像结果提交顺序、跨页面 API Key 并发保存与删除、存储位置切换，以及设置或新 Key 写入失败时的凭据隔离测试。
- 增加完成提交屏障的慢提交、提交失败和慢维护测试，并覆盖四条文字/图像完成路径、API 配置删除与换接口竞争，以及满容量补偿不覆盖后来翻译的边界。
- 增加流式译文贴底跟随、主动上滚停留，以及后续分段到达时不抢回阅读位置的单元与 Edge 端到端测试。
- 增加翻译耗时单位边界、无效值和缓存优先级测试，并在 Edge 侧栏回归中校验统一完成状态。

## 0.10.0 - 2026-08-07

### 改进

- Pi PDF 对被文字层拆散的等式、优化算子、控制字符和重复绘制数字更严格地切换到视觉识别；普通 URL 查询参数不会被误判为公式。
- 公式划选截图会保留用户实际拖动经过的范围；即使 PDF 文字层漏掉了多基线碎片或右侧编号，也不会自动扩展到用户手势之外的同行正文。
- 视觉模型已完整识别、但在译文字段中漏写单个位于原文末尾的独立行间公式时，会在本地校验后按原分隔形式补回；行内或多公式缺失仍会提示复核，不会为此重复发送图片或产生第二次计费。
- PDF 表格中由粗体叠印产生的重叠重复文字不再被当作可靠文字层，框选时会改用图像识别。
- 恢复持久 PDF 标记时会监听页面明确进入已渲染状态，修复慢速或懒加载页面上标记偶尔不出现的问题。
- Edge 原生 PDF 使用公式模型时会明确说明浏览器未提供选区图像，并引导复杂公式改用 Pi PDF 截图识别。
- 译文卡片新增紧凑的人工修改与模型调整流程，保留最近三版结果；失败后可用冻结的原文、草稿、作用范围和公式元数据安全重试。
- PDF 图像重新识别会替换同一区域的会话缓存与“本文”记忆，并同步已存在的原文标记；修正后仍可继续重新识别，不会复活旧 OCR 文本。
- API Key、模型、域名授权、Qwen OCR 与本地诊断错误分别跳转到对应设置控件；若设置页无法打开，会在当前界面给出明确反馈。
- 翻译卡片的“更多”菜单会按卡片内实际可见空间自动限高滚动，修复 PDF 标记后菜单落到画布下方、按钮可见但无法点击的问题。
- Pi PDF 工具栏新增持久的“识别本页”入口，错过扫描页提示、切换到后续页面或遇到残缺文字层时仍可手动使用；识别中可取消，换页、缩放或换文档会同步中止后台请求。
- 设置页与 OCR 适配器共用 Qwen 官方端点判定，兼容中国内地、国际站及 MaaS 地址；坐标 OCR 不受支持时会直接引导配置 Qwen，而不是只让用户更换模型名。

### 测试

- 修复翻译加载卡片替换为结果卡片时的拖拽端到端测试竞态，并增加真实论文文字层、公式截图几何、叠印表格、修订重试、图像重识别缓存、设置深链和扫描页持久入口的回归覆盖。

## 0.9.0 - 2026-08-06

### 新增

- Pi PDF 检测到无文字层的扫描页时，短暂显示“识别本页”入口；点击后只生成可调整的近整页选框，用户确认前不会截图或发送 API 请求。
- 为后续扫描 PDF 可选文字层建立严格的坐标 OCR 数据契约，校验页码、归一化坐标、唯一阅读顺序、置信度、块类型和文本长度；低置信度内容与表格不会进入可选层。
- 接入官方 Qwen OCR `advanced_recognition` 坐标结果：确认扫描页区域后可生成当前标签页内的临时可选文字层，缩放时重建，切换 PDF 后立即清除。

### 改进

- Qwen / DashScope 的 PDF 图像翻译继续使用流式输出，但不再强制发送容易触发服务端生成中止的 `response_format`；未知兼容接口仅在尚未产生任何译文时执行一次参数降级，并记住兼容性结果。
- 视觉公式识别明确保留黑板粗体、粗体、书法体和直立体等数学字体语义；兼容 `\mathop{\rm argmin}`、紧凑 `\argmin` 等模型输出，并确保优化约束在行间公式中显示于算子正下方。
- 译文中的 `**加粗**`、`__加粗__` 与三星号强调现在会以安全的轻量样式显示，并支持强调文字中夹有 LaTeX 公式；不会解析任意 HTML，复制与导出仍保留模型返回的原始文本。
- 翻译卡片与 Edge 原生 PDF 侧栏共用同一套结果展示逻辑；完整译文和逐句对照中的公式会跨容器批量生成 MathML，并对会话内重复公式使用有界缓存，减少长公式结果完成后的渲染等待。
- 长公式中唯一的 `\\tag{...}` 编号会固定显示在可滚动公式主体右侧，兼容 `aligned` 等环境并自动清理双括号与重复尾号；同一环境存在多个编号时继续使用本地渲染器的原生布局。
- PDF 图像公式翻译取消结构校验后的隐式二次识别和自动扩大截图重试：首个安全结果立即显示，未通过校验时仅提示人工核对，避免额外等待与 API 计费。
- 本地修复视觉模型 JSON 中被转义破坏的 LaTeX 反斜杠，以及 `\\textbbQ`、`\\textOmega` 等常见伪 TeX，减少公式识别结果需要人工返工的情况。
- 扫描 PDF 临时文字层新增学术排版可靠性处理：识别跨栏标题与脚注，证据充分时按双栏阅读顺序排列；公式、表格和明显旋转文字不进入可选层，继续回退到框选视觉翻译。
- 分离“正在编辑的 API 配置”和“当前文字翻译 API”：接入第二个 Qwen 等视觉接口时，现有 DeepSeek 等文字接口保持不变；每个配置仅用一行紧凑文字显示其用途，并可显式设为文字翻译。
- 保存多 API 设置时分别维护文字与 PDF 图像用途、模型和域名权限，切换 API Key 存储位置时会完整迁移各配置的 Key。
- 设置页明确区分必需的文字翻译 API 与可选的 PDF 图像能力，并提供紧凑的“配置 Qwen”入口；会自动填写官方地址和建议模型，同时保留现有文字翻译配置与自定义接口。
- 快捷面板可继承 Overleaf 当前 PDF 预览或下载地址；PDF 右键翻译按产生选区的 frame 精确投递，无法注入的受保护预览只降级一次，避免重复请求。
- 本地文件 URL 或在线 PDF 站点权限不足时，快捷面板和 Edge PDF 侧栏会显示持久操作指引；后台打开阅读器前也会统一预检，不再打开一个注定读取失败的标签页。
- Pi PDF 的 `Ctrl + 滚轮`与触控板捏合只缩放鼠标所在的 PDF 文档区域，不再放大整张阅读器页面；连续手势会合并重绘并保持当前阅读位置。

### 隐私

- 整页识别沿用现有框选限制：只发送用户最终确认的当前页区域，不发送其他页面、完整 PDF 或未确认选框。
- 坐标 OCR 只接受官方原生 `ocr_result.words_info`，不会把通用视觉模型自行生成的 JSON 坐标当作文字层；截图、识别文字和坐标均不写入扩展存储或诊断报告。

## 0.8.2 - 2026-08-04

### 改进

- API 设置调整为“服务商、API Key、连接并自动配置”三步流程，并保留自定义 OpenAI 兼容接口。
- 连接后自动读取当前 Key 可见模型、推荐并验证文字模型，并在可用时检测 PDF 图片输入模型。
- 设置页使用紧凑的能力摘要显示文字翻译、结构化输出和 PDF 图片翻译状态，不增加翻译卡片的常驻控件。
- 更新 Edge 商店单一用途、权限、数据披露和审核测试说明，为审核员提供明确的临时凭证配置与测试步骤。
- 更新透明背景商店 Logo、小型宣传图和大型宣传图。

### 安全与隐私

- 自动配置只访问用户选择的服务商或明确填写的自定义接口，不会把 API Key 发送到其他域名猜测服务商。
- API Key 继续默认保存在浏览器会话中；审核专用 Key 不包含在源码、GitHub Release 或 Edge 安装包中。
- 发布包通过密钥模式扫描和生产依赖安全审计。

## 0.8.1 - 2026-08-03

### 新增

- 增加按文档隔离的“本文”记忆：保存最近译句、固定译法与待确认术语；候选译法可在侧栏中直接修改后采用。
- 增加译文 LaTeX 自动渲染与“源码/公式”轻量切换；复制和导出始终保留原始 LaTeX。
- 增加网页渲染公式捕获，以及 PDF 公式/低清文本的多模态识别与公式 LaTeX 输出。
- 增加 Pi PDF 连续框选、区域翻译队列、原文复制、阅读状态恢复和本文标记导航。

### 改进

- PDF 划词吸附更尊重用户选区，限制跨栏、跨段、页眉页脚及整页异常扩张，按住 Alt 可临时精细选择。
- 翻译中固定到侧栏后继续保持流式输出；长文支持分段检查点、恢复和更及时的完成状态。
- 公式结构校验采用本地优先恢复和一次限时纠正，避免轻微格式差异引发连续重试与连接超时。
- 兼容 `cases` 等多行环境内的公式编号：仅修正渲染副本，复制的 LaTeX 源码不变。
- Edge 原生 PDF 与 Pi PDF 的侧栏、页面状态和文档数据进一步隔离，关闭侧栏后阅读区自动恢复居中。

### 隐私

- API Key、PDF 截图和正文不写入源码、发布包或诊断报告。
- 文档记忆使用本机匿名文档标识并设置数量、文本长度与总存储预算。

## 0.8.0 - 2026-07-30

### 新增

- Pi PDF 阅读器：在线与本地 PDF、懒加载、缩放、适合宽度、文字划选和框选翻译。
- Edge 原生 PDF 右键翻译与按标签页、按文档隔离的原生侧边栏。
- 兼容 OpenAI Chat Completions 的自定义 API 配置、模型读取、能力记忆及文字/视觉 API 分离。
- 普通文本流式输出、长文分段续翻、逐句对照和会话缓存。
- 原文轻标记、悬停译文、可选 Pi PDF 持久保存及“本文标记”导航器。
- 本文标记支持按页查看、返回原句、复制全部、复制或删除单条，并在位置变化时安全失效。
- 翻译结果底部使用明确的“标记/已标记”按钮；未开启持久保存时可从菜单一次完成“标记当前译句并保存”。

### 改进

- 多模态公式结果新增 LaTeX 结构、一致性与环境配对校验；校验失败会自动纠正一次，仍不确定时保留译文并明确提示核对。
- Pi PDF 公式识别在纠正仍不确定时自动扩大截图上下文再试一次，不增加新的常驻控件。
- Pi PDF 拖选新增完整句子吸附；短选区仍吸附到完整单词，按住 Alt 可临时关闭吸附进行精细选择。
- 双栏 PDF 的智能选区锁定起始栏，正文选区不会自动扩展到页眉和页脚。
- PDF 文字定位兼容空格变化、软连字符、换行断词和 Unicode 连字；重复句仍要求前后文唯一匹配。
- 100 页、100 条持久标记下只计算视口附近的标记位置，并继续只保留附近 5 页的 Canvas 与文字层。
- 末页跳转改用可见面积辅助判定，避免跳到最后一页后页码仍显示上一页。
- 持久标记限制为每篇最多 100 条、最近最多 30 篇，并增加单条长度和整体存储预算保护。
- 翻译卡片、侧栏和设置页保持紧凑布局，更多操作继续收纳在现有菜单中。

### 隐私

- API Key、PDF 截图和用户正文不会写入源码或诊断报告。
- 普通网页和 Edge 原生 PDF 的轻标记不持久保存。
- Pi PDF 持久标记由用户按文档主动开启，使用本机匿名文档标识，可停止、逐条删除或全部清除。
